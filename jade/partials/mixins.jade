mixin section(name)
  section.div(class=name)
    .content-wrap
      block

mixin checkbox(id, name, label)
  input(type="checkbox" id=id name=name)
  label(for=id)
    span= label

mixin horizon(title, classname)
  fieldset(class=classname)
    legend= title

